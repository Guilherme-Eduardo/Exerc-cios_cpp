#include "biblaureano.h"
int main () {

    float indicePoluicao, grupo1, grupo2, grupo3;

    cout << "Analise do indice de poluicao" << endl;
    indicePoluica = readFloat("Informe o indice de poluicao [0.01 até 0.10]: ");

    if(indicePoluicao = < 0.05){
        cout << "O indice de poluicao das empresas esta no padrao adequado" << endl;
    }
    else{
        if(indicePoluicao = < 0.5 && > 0.25){
            cout << "Erro" << endl;
        }
    }


return 0;
}
